# qrloop web exporter

Vite demo that turns text or a binary file into a looping QR stream.

```bash
# from repo root
yarn build
cd examples/web-exporter
yarn
yarn dev
```

Open the URL Vite prints, paste text or pick a file, then start the loop. Pair with [`../web-importer`](../web-importer) on another device.
